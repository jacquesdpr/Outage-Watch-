{
  "name": "legal-assistant-function",
  "version": "1.0.0",
  "private": true,
  "description": "Azure Function relay for the Marite Connect Sectional Title Assistant. Holds the Anthropic API key, searches the Sectional Title Legal Library in SharePoint via Microsoft Graph, and returns a grounded, cited answer.",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "func start",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@azure/functions": "^4.5.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pdf-parse": "^1.1.4",
    "typescript": "^5.5.0"
  }
}
